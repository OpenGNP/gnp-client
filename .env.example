VITE_API_URL=http://localhost:3000/api

# Dev auto-login: the client signs in with these on startup (see src/lib/auth.tsx).
# Matches the seed account created by `bun run seed` in gnp-server.
VITE_DEV_EMAIL=admin@sit.kmutt.ac.th
VITE_DEV_PASSWORD=
