VITE_API_URL=http://localhost:3000/api

# Optional dev convenience: if set, the client auto-signs-in with these on startup
# instead of showing the login page (see devAutoLogin in src/lib/auth.ts). Unset
# them locally if you want to exercise the login page itself.
# Matches the seed account created by `bun run seed` in gnp-server.
VITE_DEV_EMAIL=admin@sit.kmutt.ac.th
VITE_DEV_PASSWORD=
